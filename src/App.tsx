import { useEffect, useState } from "react"

export default function App() {

  const [data,setData]=useState(null);

  useEffect(()=>{
         
  },[])

  return (
    <div>
      
    </div>
  )
}
